# rtm_cp

Analysis workspace for CP experiment data (R + Python).

## Quick start

1. Create/activate the Python environment.
2. Install dependencies:

```bash
pip install -r requirements.txt
```

3. Run analysis scripts from their respective folders.
